<div class="footer">



        <div class="d-flex flex-column justify-content-center align-items-center">
            <%= link_to challenges_path, class: "p nav-link" do %>
                <i class="fa-brands fa-nfc-symbol"></i>
                Community
            <% end %>
        </div>
        <div class="d-flex flex-column justify-content-center align-items-center ">
            <%= link_to challenges_path, class: "p nav-link" do %>
                <i class="fa-solid fa-magnifying-glass"></i>
                Search
            <% end %>
        </div>
        <div class="d-flex flex-column justify-content-center align-items-center ">
            <%= link_to challenges_path, class: "p nav-link" do %>
                <i class="fa-solid fa-graduation-cap"></i>
                Learn
            <% end %>
        </div>
        <div class="d-flex flex-column justify-content-center align-items-center ">
            <ul class="navbar-nav me-auto">
                <% if user_signed_in? %>
                <%= link_to profile_path(current_user), class: "navbar-brand" do %>
                    <%= cl_image_tag current_user.photo.key, class: "p avatar", alt: "dropdown menu" %>
                <% end %>
                <% else %>
                    <div class="d-flex flex-column justify-content-center align-items-center ">
                        <i class="fa-regular fa-user"></i>
                        <%= link_to "Login", new_user_session_path, class: "p nav-link" %>
                    </div>
                <% end %>
            </ul>
        </div>


</div>
